# Copy to .env and fill with your project keys (Settings → API in Supabase dashboard).
# Restart `npm run dev` after changing.

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
